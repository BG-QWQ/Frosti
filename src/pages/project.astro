---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { Icon } from "astro-icon/components";
import MainCard from "@/components/MainCard.astro";
import GitHubStats from "@/components/mdx/GitHubStats.astro";
import RepositoryCard from "@/components/mdx/RepositoryCard.astro";
---

<BaseLayout title="Projects">
  <MainCard
    title="项目"
    description="A showcase of my open-source projects and contributions on GitHub."
    textOverlay="PROJECTS"
    infoIcon="lucide:code-2"
  >
    <!-- Featured Projects -->
    <section class="py-4">
      <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
        <Icon name="lucide:star" class="w-6 h-6 text-primary" />
        <span>特色项目</span>
      </h2>

      <GitHubStats username="1502538344" repositoryName="Frosti" showLanguages={true} showContributors={true} />
    </section>

    <!-- My Repositories -->
    <section class="py-4">
      <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
        <Icon name="lucide:folder" class="w-6 h-6 text-primary" />
        <span>我的仓库</span>
      </h2>

      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        <RepositoryCard repo="1502538344/Frosti" isPinned={true} />
        <RepositoryCard repo="1502538344/SaroProck" isPinned={true} />
        <RepositoryCard repo="1502538344/astion-static" />
        <RepositoryCard repo="1502538344/astro-update-theme" />
        <RepositoryCard repo="1502538344/Word-testing-application" />
        <RepositoryCard repo="1502538344/Smooth-Snake-Game" />
      </div>

      <div class="flex justify-center mt-10">
        <a
          href="https://github.com/1502538344?tab=repositories"
          target="_blank"
          rel="noopener noreferrer"
          class="btn btn-outline gap-2"
        >
          <Icon name="lucide:github" class="w-5 h-5" />
          <span>在 GitHub 上查看更多</span>
        </a>
      </div>
    </section>

    <!-- Open Source Contributions -->
    <section class="py-4">
      <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
        <Icon name="lucide:git-pull-request" class="w-6 h-6 text-primary" />
        <span>开源贡献</span>
      </h2>

      <div class="card bg-base-200 p-6">
        <ul class="space-y-6">
          {
            [
              {
                project: "这是项目名称，但我如今做不到，所以没有",
                description: "这里可以写贡献了什么，但我没有，又不想删除这个东西，所以添加这段话来保留",
                url: "https://github.com/1502538344/Frosti",
              },
              {
                project: "这是项目名称，但我如今做不到，所以没有",
                description: "这里可以写贡献了什么，但我没有，又不想删除这个东西，所以添加这段话来保留",
                url: "这里填项目地址",
              },
              {
                project: "这是项目名称，但我如今做不到，所以没有",
                description: "这里可以写贡献了什么，但我没有，又不想删除这个东西，所以添加这段话来保留",
                url: "这里填项目地址",
              },
            ].map((contrib) => (
              <li class="flex items-start gap-4">
                <div class="flex-shrink-0 bg-base-300 p-2 rounded-full">
                  <Icon name="lucide:git-commit" class="w-6 h-6 text-primary" />
                </div>
                <div>
                  <h3 class="text-lg font-bold">
                    <a
                      href={contrib.url}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="hover:text-primary transition-colors flex items-center gap-1"
                    >
                      {contrib.project}
                      <Icon name="lucide:external-link" class="w-4 h-4" />
                    </a>
                  </h3>
                  <p class="text-base-content/80">{contrib.description}</p>
                </div>
              </li>
            ))
          }
        </ul>
      </div>
    </section>
  </MainCard>
</BaseLayout>
